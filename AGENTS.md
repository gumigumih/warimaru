# AGENTS.md

## 開発ルール

- コミットメッセージ、PRタイトル本文は開発ルールに則ること。
- マルチエージェントを使ったほうが早いかどうか考えて、適切な場合のみ使うこと。

## ツール設計の考え方

- 「ツール」として分けるのは、ユーザーの入力の考え方や精算対象の組み立て方が根本的に違うものにする。
  - 例: 総額を等分して割る。
  - 例: 食べた分だけで割る。
  - 例: 項目ごとに対象者を変えて割る。
  - 例: 会計前に税込・割引込みで割る。
- 係数、端数調整、途中参加、子ども料金のような「割り方の調整」は、原則として別ツールにせず、各ツール内の計算オプションやモードとして扱う。
- 単純に支払い対象ではない人は、入力しないだけでよい。除外機能として扱うのは、結果共有で名前だけ残したい場合や、項目ごとに対象者が変わる場合に限る。
- 新しい機能を考えるときは、まず「これは別ツールか、既存ツールの計算オプションか」を判断する。
- 判断に迷った場合は、ユーザーが最初に入力したいものが何かを基準にする。
  - 総額から始めるなら、総額割り勘系のツール。
  - 料理・品目から始めるなら、食べた分だけ割り系のツール。
  - 費用項目と対象者の組み合わせから始めるなら、項目別精算系のツール。
  - 税・割引・サービス料など会計条件から始めるなら、会計補助系のツール。

## ツール画面の共通ステップ

- 各ツールは原則として「入力」→「途中入力・調整」→「結果・共有」の流れに揃える。
- 途中入力・調整が不要なツールでは、そのステップをスキップして「入力」→「結果・共有」にする。
- 画像保存とURL共有は「結果・共有」画面に置く。
- 共有URLから開いた場合は、入力値や計算条件を復元して同じ結果画面を表示できるようにする。
